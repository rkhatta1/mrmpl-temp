import SiteShell from "./site-shell";
import NotFound from "@/features/site/pages/NotFound";

export default function NotFoundPage() {
  return (
    <SiteShell>
      <NotFound />
    </SiteShell>
  );
}
